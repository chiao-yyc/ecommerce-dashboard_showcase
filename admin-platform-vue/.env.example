# =========================================
# 電商管理平台 - 環境變數範本
# =========================================
# 複製此檔案為 .env.local（開發）或 .env.production.local（生產）
# 並填入實際的環境變數值

# =========================================
# ⚠️ 安全警告
# =========================================
# 1. ❌ 絕對不要將 .env.local 或 .env.production.local 加入版本控制
# 2. ❌ SERVICE_ROLE_KEY 擁有完整資料庫權限，僅能在後端使用，絕不可暴露於前端
# 3. ⚠️  ANON_KEY 設計為公開使用，但必須確保 RLS 政策正確設定
# 4. 🔒 第三方 API 金鑰（OpenAI、SendGrid 等）應使用環境變數或 Secret Manager
# 5. 🔄 定期輪換敏感金鑰，特別是 SERVICE_ROLE_KEY（建議每季度）
# 6. 🔐 考慮使用 .env.vault (dotenvx) 加密敏感環境變數
# 7. ✅ 使用 CI/CD 平台的環境變數注入，避免在代碼庫中儲存敏感資料
#
# 📚 更多資訊: docs/deployment/CLOUD_DEPLOYMENT_CHECKLIST.md

# -----------------------------------------
# Supabase 連線配置（前端使用）
# -----------------------------------------

# Supabase API URL
# 本地開發: http://localhost:54321 (Supabase CLI)
# 生產環境: https://api.your-domain.com（雲端 Supabase）
VITE_SUPABASE_URL=

# Supabase 匿名金鑰（Anon Key）
# 本地開發: 使用 `supabase status` 取得
# 生產環境: 從雲端 Supabase 部署配置取得
VITE_SUPABASE_ANON_KEY=

# Supabase Storage Bucket 名稱
# 預設: ecommerce-dashboard
VITE_SUPABASE_BUCKET_NAME=ecommerce-dashboard

# -----------------------------------------
# 應用程式基礎 URL
# -----------------------------------------

# 前端應用基礎 URL
# 用於組建 OAuth 回調、密碼重設等前端頁面 URL
# 本地開發: http://localhost:5173
# 生產環境: https://your-domain.com
VITE_BASE_URL=

# 認證回調基礎 URL（可選）
# 如果需要與 VITE_BASE_URL 不同的認證回調 URL，可設定此變數
# 預設使用 VITE_BASE_URL
# VITE_AUTH_BASE_URL=

# 認證回調路徑（可選）
# 預設: /auth/callback
# VITE_AUTH_CALLBACK_PATH=/auth/callback

# 完整認證回調 URL（可選）
# 優先度: VITE_AUTH_CALLBACK_URL > VITE_AUTH_CALLBACK_PATH > VITE_BASE_URL
# VITE_AUTH_CALLBACK_URL=

# -----------------------------------------
# Supabase 後端服務配置
# -----------------------------------------

# Supabase 後端服務 URL
# 用於認證重定向白名單和郵件 URL 構建
# 本地開發: http://localhost:5173
# 生產環境: https://your-domain.com（需與雲端 Supabase 配置一致）
SITE_URL=

# -----------------------------------------
# ⚠️ 絕不可加入 Git 的變數（僅後端使用）
# -----------------------------------------

# Supabase Service Role Key（完整資料庫權限）
# ❌ 警告: 此金鑰可繞過所有 RLS 政策，絕對不可洩漏或暴露於前端
# ❌ 僅能在後端服務、Edge Functions、管理腳本中使用
# 🔒 請妥善保管，定期輪換（建議每季度）
# VITE_SUPABASE_SERVICE_ROLE_KEY=

# 資料庫直連字串（完整存取權限）
# ❌ 警告: 包含資料庫密碼，絕對不可洩漏
# 🔒 僅用於後端服務或本地管理腳本
# SUPABASE_DB_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres

# -----------------------------------------
# 第三方服務金鑰（僅後端使用）
# -----------------------------------------

# Google OAuth（如果使用 Google 登入）
# 🔒 從 Google Cloud Console 取得
# ⚠️ Secret 絕不可暴露於前端，應在 Supabase Dashboard 設定
# SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
# SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=

# SendGrid API Key（如果使用 SendGrid 發送郵件）
# 🔒 僅用於後端郵件發送服務
# SENDGRID_API_KEY=

# OpenAI API Key（如果使用 Supabase AI）
# 🔒 僅用於後端 AI 功能或 Edge Functions
# OPENAI_API_KEY=

# -----------------------------------------
# 日誌與調試配置
# -----------------------------------------

# 日誌輸出等級
# debug - 顯示所有日誌（開發環境預設）
# info - 顯示資訊、警告、錯誤
# warn - 顯示警告和錯誤
# error - 僅顯示錯誤（生產環境預設）
# silent - 完全靜默
VITE_LOG_LEVEL=debug

# 啟用路由調試日誌
# true - 顯示路由導航、權限檢查等詳細日誌
# false - 關閉路由調試（生產環境建議）
VITE_ENABLE_ROUTER_DEBUG=true

# -----------------------------------------
# 其他配置（擴展使用）
# -----------------------------------------

# 應用程式名稱
# APP_NAME=電商管理平台

# 應用程式環境
# development / production / staging
# NODE_ENV=development
